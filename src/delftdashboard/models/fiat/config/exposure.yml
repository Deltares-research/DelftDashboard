element:
- style: tabpanel
  position:
    x: 10
    y: 10
    width: -10
    height: 160
  tab:
  - string: Asset Locations
    module: delftdashboard.models.fiat.exposure_start
    element: exposure_start.yml
  # - string: Asset Locations  # NOTE: this tab can be activated again when we use the 'start from scratch' option 
    # module: delftdashboard.models.fiat.exposure_locations
    # element: exposure_locations.yml
  - string: Classification
    module: delftdashboard.models.fiat.exposure_classification
    element: exposure_classification.yml
  - string: Finished Floor Height
    module: delftdashboard.models.fiat.exposure_finished_floor_height
    element: exposure_finished_floor_height.yml
  - string: Ground Elevation
    module: delftdashboard.models.fiat.exposure_ground_elevation
    element: exposure_ground_elevation.yml
  - string: Max Potential Damages
    module: delftdashboard.models.fiat.exposure_damages
    element: exposure_damages.yml
