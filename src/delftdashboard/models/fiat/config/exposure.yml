element:
- style: tabpanel
  position:
    x: 10
    y: 10
    width: -10
    height: 160
  tab:
  - string: NSI Quick Build
    module: models.fiat.exposure_nsi_quick_build
    element: exposure_nsi_quick_build.yml
  - string: Roads Quick Build
    module: models.fiat.exposure_roads_quick_build
    element: exposure_roads_quick_build.yml
  - string: Asset Locations
    module: models.fiat.exposure_locations
    element: exposure_locations.yml
  - string: Classification
    module: models.fiat.exposure_classification
    element: exposure_classification.yml
  - string: Asset Heights
    module: models.fiat.exposure_asset_heights
    element: exposure_asset_heights.yml
  - string: Damage values
    module: models.fiat.exposure_damages
    element: exposure_damages.yml
  - string: Additional Attributes
    module: models.fiat.exposure_aggregation
    element: exposure_aggregation.yml